<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="10dp"
    android:background="@color/snow">

    <TextView
        android:id="@+id/beizhu_tv"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/add_beizhu"
        android:textColor="@color/black"
        android:gravity="center"
        android:textSize="16sp" />

    <EditText
        android:id="@+id/beizhu_et"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_below="@id/beizhu_tv"
        android:layout_marginTop="10dp"
        android:background="@color/snow"
        android:hint="备注"
        android:inputType="text"
        android:text="@string/beizhu"
        android:textSize="14sp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="10dp"
        android:layout_below="@id/beizhu_et">

        <Button
            android:id="@+id/beizhu_btn_cancel"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/cancel"
            android:textStyle="bold"
            android:layout_marginRight="5dp" />

        <Button
            android:id="@+id/beizhu_btn_ensure"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/ensure"
            android:textStyle="bold"
            android:layout_marginRight="5dp"/>
    </LinearLayout>
</RelativeLayout>